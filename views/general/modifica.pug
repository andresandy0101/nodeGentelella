extends ../layout/layout

block content
	form(method='POST' action='/general/modifica' class="form-horizontal form-label-left" novalidate)
		include form.pug
		button(type="submit" class="btn btn-success pull-right") Modificar
		a(href='/general' class="btn btn-primary pull-right") Cancel